interface:
  display_name: "Portal Test Verification"
  short_description: "Select and run Portal SAG verification gates"
  default_prompt: "Use $test-verification to select and execute the correct checks for this Portal SAG change."
policy:
  allow_implicit_invocation: true
